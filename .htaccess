<IfModule mod_rewrite.c>
    <IfModule mod_negotiation.c>
        Options -MultiViews -Indexes
    </IfModule>

    RewriteEngine On
    RewriteBase /bold-fellows/

    RewriteCond %{REQUEST_URI} !^/bold-fellows/public/
    RewriteRule ^(.*)$ public/$1 [L]
</IfModule>
